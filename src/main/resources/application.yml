server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stanki_service?useSSL=false&serverTimezone=Europe/Moscow
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
